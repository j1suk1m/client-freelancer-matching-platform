name: contract-service ci/cd

on:
  push:
    branches:
      - main
    paths:
      - 'contract-service/**'

jobs:
  setting:
    uses: ./.github/workflows/yml/contract-yml-workflow.yml
    secrets: inherit

  releasing:
    uses: ./.github/workflows/release-workflow.yml
    secrets: inherit
    with:
      service-name: hexagon-contract-service
      service-dir: ./contract-service
      file-name: Dockerfile

  deploying:
    needs: releasing
    uses: ./.github/workflows/deploy-workflow.yml
    with:
      service-name: hexagon-contract-service
      image-tag: ${{ needs.releasing.outputs.tag_name }}
